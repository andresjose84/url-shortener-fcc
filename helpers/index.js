const createShortUrl = require( './createShortUrl' );
const findLastDoc = require( './findLastDoc' );
const findShortUrl = require( './findShortUrl' );
const findUrl = require( './findUrl' );

module.exports = {
    createShortUrl,
    findLastDoc,
    findShortUrl,
    findUrl,
}